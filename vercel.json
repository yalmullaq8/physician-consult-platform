{
  "version": 2,
  "installCommand": "if [ -f package.json ]; then npm ci; else cd frontend && npm ci; fi",
  "buildCommand": "if [ -f package.json ]; then npm run build; else cd frontend && npm run build; fi",
  "framework": "nextjs"
}